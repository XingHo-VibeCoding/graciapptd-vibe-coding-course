# 自律计划 · 运行说明（RUN.md）

- 版本：v2.0（Day 7.1 重置后重建；v1.0 已由 git 历史保留）
- 撰写日期：2026-09-23
- 依据：TECH_DESIGN.md v2.0（vanilla 单文件路线）
- 作用：任何人（包括半年后的自己）拿到仓库，照着做就能把页面跑起来

---

## 一、这个项目是什么

一个纯网页版的自律助手：时间轴待办 + 心情五选一 + 无压力思考 + 周月报。
**无后端、无安装依赖、无登录**——所有数据存在浏览器 localStorage 里（约 5MB）。

## 二、怎么跑起来（3 步）

> 前提：电脑装了 Python 3（Windows 一般自带；Mac/Linux 自带）。

**第 1 步：打开终端，进入仓库根目录**

```
cd C:\Users\Lenovo\Desktop\vibe-coding-course
```

> 关键点：必须停在 `vibe-coding-course` 这一层（仓库根），不要进 `habit-tracker/` 里面。
> 下面第 2 步的命令会以当前目录作为网站的"根"，这样页面地址和 GitHub 上的目录结构一致。

**第 2 步：启动本地服务器**

```
python -m http.server 8765
```

- `8765` 是端口号（避开常用的 80/3000/8080，减少撞车）
- 看到 `Serving HTTP on ...` 字样就是成功了
- 这个窗口**不要关**，关了网站就停了；停止服务按 `Ctrl + C`

**第 3 步：浏览器打开页面**

```
http://localhost:8765/habit-tracker/frontend/index.html
```

> `localhost` 的意思就是"本机自己"——这个地址只有你的电脑能打开，别人访问不了，数据也在你浏览器里，隐私安全。

## 三、怎么验证它没坏（对照 PRD 验收标准）

当前进度（5 步计划 Step 1-2 + Day 8 四状态 + Day 11 结构修订 + Day 12 深色落日主题与四象限）：

| 验收项 | 操作 | 预期 |
|--------|------|------|
| 首页可见 | 打开页面 | 顶部落日图片（向下渐变入黑）、标题、周历条、筛选标签、底部三个导航（今天/思考/我的），全站深色底 |
| A2 时间轴 | 输入"晨跑"，选 09:00，点 ＋ | 卡片出现在时间轴 09:00 位置；不选时间的进"未安排" |
| A5a 四象限 | 添加待办时选择象限（或不选） | 卡片带象限颜色标记：Q1 橙红 / Q2 琥珀 / Q3 蓝灰 / Q4 石板灰；不选为"未分类"雾蓝；刷新不丢 |
| A3 勾选/恢复 | 点卡片左边圆圈 | 实心+划线置灰；再点恢复；刷新页面状态不丢 |
| A4 删除 | 点卡片"···" | 弹确认框，确认后消失，刷新后仍消失 |
| A5 只读 | 点周历条里的明天 | 非今天：添加表单和 FAB 隐藏、圆圈禁用、显示"这一天只能查看" |
| 加载状态 | 打开页面瞬间 | "加载中…"闪现后进入内容 |
| 空状态 | 清掉所有待办 | "今天还没有安排，写一条试试" + 示例入口 |
| 错误状态 | （数据损坏时才出现） | "数据读取出了点问题" + 重试按钮 |
| mock 数据 | 空状态点"看看示例数据" | 今日 4 张卡片（横幅提示"仅演示不保存"），FAB 隐藏 |
| A6 心情自动弹窗 | 当天未选心情时打开页面 | 自动弹出五表情弹窗；点"先不选"可关闭且不再自动弹 |
| A7 五种心情 | 查看弹窗 | 😢伤心 / 😞忧郁 / 😐平静 / 😌惬意 / 😄喜悦 五个可选 |
| A8 安抚文案 | 选伤心或忧郁 | 情绪板块下方出现安抚句，全程无负面/警告文案 |
| A9 覆盖更新 | 进"我的"点今天格子重选 | 只保留最后一次心情，刷新后不变 |
| FAB 范围 | 切到"思考"或"我的" | 右下角 ＋ 消失；只在"今天"视图出现 |
| FAB 加待办 | 在"今天"视图点右下角 ＋ | 弹出"添加今日待办"弹窗（含象限选择），确认后进今天时间轴 |
| 我的-名字 | "我的"页点"改名" | 输入后回车保存，刷新后保持；空名回落"自律的你" |
| 我的-使用天数 | 打开"我的"页 | 显示"使用第 N 天"，从首次使用那天起算 |
| 我的-情绪板块 | 打开"我的"页 | 本周周一~周日（标题旁显示日期范围，如 9.21 - 9.27，下周一自动换周）；点今天的格子可重选心情；未到的日子显示占位（Day 16） |
| A23 待办菜单 | 点待办卡片"···" | 出现"移动到其他日期 / 删除"菜单（点空白处关闭）；删除先确认；移动选好日期确认后卡片换到目标日期（Day 13） |
| A24 思考页 | 切到"思考"页点底部中间 ＋ | 写内容保存后新卡片出现在最上面；长文（>100 字）折叠为 4 行，可展开/收起；刷新不丢（Day 14） |
| A25 AI 洞察 | "思考"页顶部点 ✨ | 弹窗五种模式切换即重新生成；输出基于本机数据；MBTI 娱乐版带免责声明；数据不上传（Day 15） |
| A26 应用页 | 点底部导航"应用" | 6 个入口列表；点"待办事项"跳回"今天"页并 Toast 提示；其余 5 个显示 Day 标签、不可点（Day 17 骨架，子功能 Day 18-22 逐个开放） |

> 报告（F4）还没做——按 R7 规则一天一块。思考（F3）卡片流 MVP（Day 14）与 AI 洞察（Day 15）已完成；应用页（F6）骨架已完成（Day 17），子功能按天开放。

## 四、常见问题（FAQ）

| 症状 | 原因 | 解法 |
|------|------|------|
| 打不开页面，浏览器一直转 | 第 2 步的服务器没启动，或窗口被关了 | 重做第 2 步 |
| 提示"端口被占用"（Address already in use） | 8765 被别的程序占了 | 换个号：`python -m http.server 8766`，地址栏也改成 8766 |
| 页面开了但样式全乱/白屏 | 没从仓库根目录启动，相对路径断了 | 回到第 1 步，确认当前目录再启动 |
| 想清空所有数据重来 | 数据在 localStorage 里 | 浏览器按 F12 → Application（应用）→ Local Storage → 删除 `habit-tracker-v1` |

## 五、代码去哪看

全部前端代码在一个文件里：`habit-tracker/frontend/index.html`（约 690 行）。
结构分五段，从上到下：CSS 样式 → HTML 骨架 → 存储层（loadData/saveData）→ 状态与渲染 → 事件绑定。
找任何功能先看注释里的 `Day 8` / `F1` 标记，和 PRD 的编号一一对应。

---

> 本文件随项目演进更新：每加一个功能（F2-F5），在第三节验收表补一行。
